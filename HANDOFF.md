# 인계 문서 — 왈왈왈 v1.10.0

크롬 확장. 웹 페이지 옆에 도트 골든 리트리버가 살면서 응원 말풍선을 띄우고,
클릭하면 질문에 답한다. NVIDIA NIM 으로 화면을 읽고 맞춤 멘트를 만든다.

- 저장소: https://github.com/aidennis0297-art/reply-immediately
- 소스: `C:\Users\denni\OneDrive\문서\walwalwal`
- 배포 zip: 부모 폴더의 `walwalwal-v1.10.0.zip`

---

## 1. 지금 어디까지 왔나

| 영역 | 상태 |
|---|---|
| 말풍선 | 좌/우/위/아래 5가지 배치, 동시 0~10개, 크기 60~140%, 여백 0~60px |
| 멘트 | 2952개 (말투 4종 각 620+, 꿀팁, 애정도 100단계) |
| 말투 | 기본 / 커뮤(디시) / 츤데레 / 선비 / 모두 — 색으로 구분 |
| AI | NIM `nemotron-3.5-lightning-30b-a3b`. 10줄 배치 + 사이트별 보관함 |
| 강아지 | 걷기·앉기·잠·귀쫑긋 + 마우스 추종 + 애정도 1000단계 |
| 질문 | 개 클릭 → 개 위 인라인 바(마이크/타자, 폭 조절, 대화 이어가기) |
| 설정 | 팝업에 전원·강아지·따라오기·대화이어가기·말투·위치·슬라이더 3개·API키·보관함 |

**미검증**: v1.9.0 의 `document_start` 전환과 답변을 개 말풍선으로 옮긴 부분은
브라우저 실동작 확인을 못 했다. 미리보기 패널이 숨겨져 타이머가 멈춘 탓
(1초 대기조차 30초 타임아웃). 코드 검증과 `node test.js` 만 통과한 상태다.
**네이버 검색 이동 시 깜빡임과 질문 답변 흐름을 실제로 확인해야 한다.**

---

## 2. 파일 지도

```
manifest.json    MV3. content script 는 document_start 주입, 단축키 없음
content.js       ~800줄. 말풍선·강아지·질문 바 전부. 여기가 본체
dog.js           도트 스프라이트(24x16 문자 그리드) + 아이콘 + 캔버스 렌더
lines.js         멘트 데이터. 자동 생성된 형태라 손으로 고치지 말 것
sw.js            NIM 호출, 애정도, 사이트별 보관함, 탭 수 조회
prompts.js       말투별 시스템 프롬프트. PERSONA + EXAMPLE + TASK/ASK 조립
popup.html/js    설정 창
test.js          node test.js — 파싱·멘트·스프라이트·애정도·보관함·패키징 검증
test.html        브라우저 확인용. chrome API 를 스텁으로 흉내
tools/subset-font.py   팝업용 서브셋 폰트 생성기
fonts/           Galmuri11.woff2(504KB, 말풍선용) / Galmuri11-ui.woff2(7KB, 팝업용)
```

---

## 3. 반드시 알아야 할 함정

여기 적힌 건 전부 실제로 한 번씩 밟은 것들이다.

### 크롬 확장
- **`_` 로 시작하는 파일명은 확장 로드를 거부당한다.** `_test.html` 때문에
  "Filenames starting with _ are reserved" 로 설치가 통째로 실패했다.
  `test.js` 가 이걸 검사한다.
- **팝업은 툴바 아이콘으로만 열린다.** `chrome://extensions` 카드를 눌러도 안 뜬다.
- **`chrome.storage` 는 분당 120회 쓰기 제한이 있다.** 슬라이더 `oninput` 에서
  매번 `set` 하면 쿼터에 걸려 값이 씹히고, 팝업을 닫았다 열어야 반영되는 것처럼 보인다.
  → `saveSoon()` 으로 180ms 모아 쓴다.
- **팝업에 큰 폰트를 물리면 창이 늦게 뜬다.** `font-display: swap` 은 흰 화면만 막을 뿐
  504KB 요청은 그대로다. 팝업 문구는 고정이니 서브셋(7KB)을 쓴다.
- **MV3 서비스 워커는 유휴 시 잠든다.** 팝업/콘텐츠에서 `sendMessage` 후
  응답이 안 올 수 있으니 항상 타임아웃 폴백을 둔다(`poke`, `sendAsk` 참고).

### 마이크 / Web Speech API
- **`SpeechRecognition.start()` 를 바로 부르면 크롬이 권한 프롬프트도 안 띄우고
  `not-allowed` 로 즉시 끝낸다.** "누르자마자 꺼진다"의 정체가 이거였다.
  → `navigator.mediaDevices.getUserMedia({audio:true})` 로 권한을 먼저 받고,
  스트림은 즉시 정지한 뒤 `start()` 한다.
- http 사이트에서는 아예 안 된다. `isSecureContext` 로 먼저 거른다.
- `no-speech` 는 흔하다. 한 번은 자동 재시도한다.

### CSS
- **`animation` 이 `transform` 을 덮어쓴다.** 가운데 정렬용
  `translate(-50%,-50%)` 가 `pop` 키프레임에 지워져 패널이 어긋났다.
  → 키프레임 안에도 같은 translate 를 넣은 `askIn` 을 따로 만들었다.
- 말풍선은 Shadow DOM 안에 있어 사이트 CSS 영향을 안 받는다. 루트에 `all:initial`.

### 멘트 고르기
- **LRU 다.** `counts[text]` 는 출력 횟수가 아니라 **마지막으로 쓴 순번**.
  안 쓴 것 → 가장 오래된 하위 12% 순으로 고른다.
- **조건 멘트를 과하게 우선하면 오히려 반복된다.** 조건에 맞는 집합이 작아서다.
  65% 로 고정했더니 중복이 38% 였다. 집합 크기 비례(최대 45%)로 바꿔 6.5% 가 됐다.
- **디바운스를 매번 리셋하면 영원히 저장되지 않는다.** `remember()` 가 호출될 때마다
  flush 타이머를 다시 걸어서, 말이 계속 나오는 동안 순번이 한 번도 안 써졌다.
  → 이미 걸린 타이머는 그대로 둔다.

### 페이지 이동
요구가 두 번 바뀌었다. 지금 규칙은 이렇다.
- 앞 화면 기준으로 받아둔 **AI 대사 큐는 버린다**.
- 이미 **떠 있는 말풍선은 지우지 않는다** — 지우면 눈에 띄게 끊긴다.
- **개는 있던 자리에서 이어진다** (`dogPos` 에 위치·방향·상태 저장).
- 새 문서에서는 120ms 뒤 바로 채우고, 비어 있으면 3개까지 한 번에 올린다.

### 개발 환경 (이 저장소 특유)
- **Bash heredoc 이 긴 내용에서 깨진다.** 200줄 넘는 JS/파이썬을 `<<'EOF'` 로
  넣으면 `unexpected EOF` 가 난다. → Write 로 스크립트 파일을 만들고 실행한다.
  `scratchpad/*.py` 가 그렇게 쓰던 것들이다.
- **heredoc 안에서 정규식 백슬래시가 사라진다.** `/\n+/` 가 실제 개행으로,
  `[^'\\]` 가 `[^'\]` 로 바뀌어 문법 오류가 났다. → 파일로 쓰거나 `chr(92)` 로 조립.
- **PowerShell here-string 으로 `git commit -m` 하면 인자가 쪼개진다.**
  → 메시지를 파일로 쓰고 `git commit -F <file>`.
- **미리보기 패널이 숨겨지면 타이머가 멈춘다.** `setTimeout(1300)` 이 30초를 넘긴다.
  애니메이션도 첫 프레임에 고정되어 `getBoundingClientRect` 가 `scale(.4)` 값을 준다
  (340px 가 136px 로 측정됨). 좌표를 잴 땐 `element.style` 값을 직접 읽는 게 낫다.

---

## 4. 검증 방법

```bash
node test.js                  # 항상 먼저. 파싱·멘트·스프라이트·애정도·패키징
python tools/subset-font.py   # 팝업 문구를 바꿨을 때만
```

브라우저 확인은 `test.html` 을 로컬 서버로 열어서 한다.
`.claude/launch.json` 에 `walwalwal` 항목(python http.server 8777)이 있다.
스텁이 `chrome.storage` 를 localStorage 로 흉내내서 리로드해도 값이 남는다.
스크립트는 `?t=` 로 캐시를 무력화한다 — 안 그러면 옛 파일이 계속 물린다.

---

## 5. 다음에 하면 좋을 것

1. **v1.9.0 실동작 확인** (위 "미검증" 참고). 이게 제일 급하다.
2. **API 키가 없는 첫 사용자 경험** — 지금은 팝업 안내뿐이다. 개가 먼저
   "키 넣어줘" 라고 말해주면 친절할 것.
3. **`lines.js` 가 자동 생성 형태**라 사람이 읽고 고치기 나쁘다.
   카테고리 주석이 사라졌다. 다시 정리하려면 생성 스크립트를 만드는 편이 낫다.
4. **`content.js` 가 800줄**이다. 말풍선/개/질문바로 쪼갤 수 있지만,
   content script 는 파일이 늘면 주입 순서를 신경 써야 한다. 서두를 일은 아니다.
5. **애정도 100단계 멘트**가 1000번 클릭해야 다 보인다. 팝업에서 미리보기를
   제공하면 재미가 살 것.

---

## 6. API 키

NVIDIA NIM 무료 키(`nvapi-` 로 시작)를 팝업에서 넣는다.
**코드와 zip 어디에도 키는 없다.** `chrome.storage.local` 에만 저장된다.
키가 없으면 내장 멘트 2952개로만 동작한다.

호출 절약: 최소 40초 간격 + 페이지별 5분 캐시 + 사이트별 보관함(호스트당 40줄,
80개 사이트). 쿨다운 중에는 보관함에서 꺼내 쓴다.
